<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<model xmlns="http://www.neeveresearch.com/schema/x-ddl">
    <env>
        <nv>
            <stats>
                <memory>
                    <enable>true</enable>
                    <type>
                        <enable>true</enable>
                    </type>
                </memory>

                <latency>
                    <waypoint>
                        <global>
                            <enabled>true</enabled>
                        </global>
                    </waypoint>
                    <leg>
                        <global>
                            <enabled>true</enabled>
                        </global>
                        <messaging>
                            <type>
                                <enabled>true</enabled>
                            </type>
                        </messaging>
                    </leg>
                </latency>
            </stats>
        </nv>
        <{{AppTokenName}}>
        </{{AppTokenName}}>
    </env>

    <buses>
        <bus name="{{BusName}}" descriptor="${SYSTEM_BUS_DESCRIPTOR}"/>
    </buses>

    <apps>
        <templates>
        </templates>
    </apps>

    <xvms>
        <templates>
        </templates>
    </xvms>

    <profiles>
        <profile name="cloud">
            <activation>
                <properties>
                    <rumi.agent.env>cloud</rumi.agent.env>
                </properties>
            </activation>
            <env>
                <ROOT_DIR>${{{AppTokenName}}.rootdir::/home/rumi/{{AppTokenName}}}</ROOT_DIR>
                <SYSTEM_BUS_DESCRIPTOR>{{MessagingConnectionString}}</SYSTEM_BUS_DESCRIPTOR>
                <STORE_LOCATION>${ROOT_DIR}/store/${nv.ddl.targetxvm}</STORE_LOCATION>
                <nv>
                    <!-- discovery -->
                    <discovery.descriptor>activemq://discovery.rumi.local:61616?wireFormat.maxInactivityDuration=0</discovery.descriptor>
                </nv>

                <{{AppTokenName}}>
                </{{AppTokenName}}>
            </env>

            <apps>
                <templates>
                </templates>
            </apps>

            <xvms>
                <templates>
                </templates>
            </xvms>
        </profile>
    </profiles>
</model>
